# Production values; adjust target to your API host if not same-origin
VITE_API_BASE_URL=/api
# Vite dev proxy is ignored in prod; this is for local dev only
VITE_DEV_PROXY_TARGET=http://localhost:8080
