spring:
  application:
    name: todolist
  datasource:
    url: jdbc:postgresql://localhost:5432/todolist
    driver-class-name: org.postgresql.Driver
    username: app
    password: secret
  jackson:
    serialization:
      WRITE_DATES_AS_TIMESTAMPS: false

server:
  port: 8080

logging:
  level:
    root: INFO
    org.springframework.web: INFO
    org.hibernate.SQL: WARN

# Custom JWT and CORS settings (kept from properties)
jwt:
  secret: 13eOjalvg4QBjotrBt7aWP/egE/Ul7qrpTpAnGglqpVhXilzdQbaafmZSU+ktN1aI6Iiuelg+27U437ERFpvIA==
  access:
    ttl-seconds: 900
  refresh:
    ttl-seconds: 1209600

app:
  cors:
    allowed-origin: http://localhost:5173
  cookies:
    secure: false
  refresh:
    fallback-enabled: true
